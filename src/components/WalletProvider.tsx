"use client";

import type { ReactNode } from "react";

/** Placeholder for future wallet/RPC context. Layout expects this wrapper. */
export function WalletProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
